<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 앱 기본 테마 -->
    <style name="Theme.CromaScan" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">#4F3A78</item>
        <item name="android:statusBarColor">#4F3A78</item>
        <item name="android:navigationBarColor">#4F3A78</item>
    </style>

    <!-- 팝업 메뉴 테마 (옵션) -->
    <style name="Theme.CromaScan.PopupMenu" parent="ThemeOverlay.Material3.Light" />

    <!-- 스플래시 테마 -->
    <style name="Theme.CromaScan.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

</resources>
